## Already done:

- recursive root directory indexing
- fuzzy file search
- root content grep
- second extract-cache grep
- plain/regex/fuzzy modes
- merge native + extracted results
- `.txt` extract path remapping
- background scan
- filesystem watcher
- rescan trigger
- default CLI limit `20`

## Missing:

- `SEARCH_ROOT` environment config
- `EXTRACT_CACHE_DIR` environment config
- PDF extraction
- Office extraction through LibreOffice
- extract cache hashing
- extract cache upsert
- stale extract pruning
- document index orchestration
- automatic extraction when files change
- long-running backend ownership; CLI currently creates service, runs command, exits
